你现在是我的全栈开发助手。我们正在开发一个“小红书任务审核与分销系统”。

当前技术栈：Node.js + Express + MongoDB + React + 微信小程序。

核心业务逻辑约束（不可违反）：
1. 架构：单体应用，财务与业务逻辑代码分离但库不分离。
2. 结算：实行“一级分销”，提交任务时必须写入“快照价格(snapshot_price)”。
3. 风控：图片上传必须校验 MD5 去重。
4. 流程：用户提交(0) -> 客服一审(1) -> 老板二审(2) -> 财务打款(3)。
5. 资金：只有状态变为 2 (待打款) 时，系统才生成 Transaction 记录。

请基于以上上下文，帮我完成以下任务：
[在这里输入你想让它写的代码，比如：帮我写老板批量确认的接口]