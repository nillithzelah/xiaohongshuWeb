/* pages/index/index.wxss */
page {
  background-color: #f5f7fa;
}

.container {
  padding-bottom: 40rpx;
}

/* 🖼️ 画廊轮播图样式 */
.banner-container {
  padding-top: 20rpx;
  background-color: #f5f7fa; /* 保持背景色一致 */
}

.card-swiper {
  height: 360rpx; /* 轮播图高度 */
}

.card-swiper-item {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 10rpx 0; /* 上下留点空隙给阴影 */
}

.swiper-box {
  width: 100%;
  height: 100%;
  border-radius: 16rpx;
  overflow: hidden;
  transform: scale(0.92); /* 默认缩小一点 */
  transition: all 0.2s ease-in 0s;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  position: relative;
  margin: 0 10rpx; /* 图片之间的间距 */
}

/* 当前选中的图片放大 */
.swiper-box.cur {
  transform: scale(1);
  box-shadow: 0 8rpx 20rpx rgba(24, 144, 255, 0.3); /* 选中时加深阴影 */
  z-index: 10;
}

.swiper-image {
  width: 100%;
  height: 100%;
  border-radius: 16rpx;
}

/* 图片上的文字标题 */
.swiper-text {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 60rpx;
  line-height: 60rpx;
  background: rgba(0, 0, 0, 0.4);
  color: #fff;
  font-size: 24rpx;
  padding-left: 20rpx;
  backdrop-filter: blur(4px);
}

/* 👤 头部用户信息卡片 */
.header-card {
  background: #fff;
  padding: 30rpx;
  margin: 20rpx 30rpx;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.05);
}

.user-info {
  display: flex;
  align-items: center;
}

.avatar {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  border: 4rpx solid #f0f0f0;
}

.user-text {
  margin-left: 24rpx;
}

.greeting {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.subtitle {
  font-size: 24rpx;
  color: #999;
  margin-top: 8rpx;
}

/* 📋 列表样式 */
.list-container {
  padding: 0 30rpx;
}

.list-header {
  font-size: 28rpx;
  color: #666;
  margin-bottom: 20rpx;
  margin-left: 10rpx;
}

.review-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  display: flex;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.03);
}

.card-thumb {
  width: 120rpx;
  height: 120rpx;
  border-radius: 12rpx;
  background: #f0f0f0;
  flex-shrink: 0;
}

.card-content {
  flex: 1;
  margin-left: 24rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.card-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.task-type {
  font-size: 30rpx;
  font-weight: bold;
  color: #333;
}

.time-row {
  margin-top: 8rpx;
}

.submit-time {
  font-size: 24rpx;
  color: #999;
}

.amount {
  font-size: 32rpx;
  color: #52c41a;
  font-weight: bold;
}

/* 状态标签颜色 */
.status-tag {
  font-size: 22rpx;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
}
.status-tag.pending, .status-tag.cs_review, .status-tag.boss_approved {
  background: #e6f7ff;
  color: #1890ff;
}
.status-tag.completed, .status-tag.finance_done {
  background: #f6ffed;
  color: #52c41a;
}
.status-tag.rejected {
  background: #fff2f0;
  color: #f5222d;
}

.reject-reason {
  margin-top: 12rpx;
  background: #fff2f0;
  color: #ff4d4f;
  font-size: 22rpx;
  padding: 8rpx 12rpx;
  border-radius: 8rpx;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 100rpx 0;
}

.empty-icon {
  width: 150rpx;
  margin-bottom: 20rpx;
}

.go-upload-btn {
  margin-top: 30rpx;
  background: #1890ff;
  color: #fff;
  font-size: 28rpx;
  border-radius: 40rpx;
  padding: 0 60rpx;
}

.loading-more {
  text-align: center;
  color: #ccc;
  font-size: 24rpx;
  padding: 20rpx 0;
}